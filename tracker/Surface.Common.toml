kind = 'area'
title = 'Shared surface setup'
parent = 'Surface'
notes = '''
The setup every `ℝⁿ` surface shares: the Euclidean instantiation of the backbone's dual pair, so
that a book working in coordinates does not restate it.
'''
